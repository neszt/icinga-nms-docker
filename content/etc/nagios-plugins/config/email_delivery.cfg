define command {
        command_name    check_email_delivery
        command_line     /usr/lib/nagios/plugins/check_email_delivery -H '$HOSTADDRESS$' --mailto '$ARG1$' --mailfrom '$ARG1$' --body "teszt from nagios plugin" --username '$ARG1$' --password '$ARG2$'   --smtptls --imapssl --imap-retries 60
}
